# ChatGPT Project Instruction — /audit-ui Visual Concepts

Paste this into ChatGPT Project instructions when the project should support `/audit-ui`.

## Strong version

```txt
CRITICAL for `/audit-ui`: if ChatGPT image generation is available, you MUST call the image generation tool and create 3 separate visual mockups before the final answer: Concept A image, Concept B image, Concept C image. Do not replace images with text wireframes. Follow shared docs from `andylitvinov-design/reiki-yggdrasil`: `docs/audit-ui-mode.md`, `docs/audit-ui-expert-frameworks.md`, `docs/audit-ui-decision-rubric.md`, `docs/audit-ui-completeness-gate.md`, `docs/audit-ui-visual-artifact-contract.md`. If image generation is unavailable, explicitly write `VISUAL_ARTIFACT_UNAVAILABLE` and then provide 3 structured wireframes. Do not implement code until I choose Concept A/B/C or run `/delivery`.
```

## Short version

```txt
/audit-ui: MUST generate 3 visual concept images with image generation when available (Concept A/B/C) before final answer. If image generation is unavailable, write `VISUAL_ARTIFACT_UNAVAILABLE` + 3 wireframes. Use shared docs from `andylitvinov-design/reiki-yggdrasil`. No code until Concept A/B/C is chosen or `/delivery` is run.
```
