# ChatGPT Project Instruction — /audit-ui Visual Concepts

Paste this into ChatGPT Project instructions when the project should support `/audit-ui`.

```txt
/audit-ui must follow the shared UI audit docs in `andylitvinov-design/reiki-yggdrasil`: `docs/audit-ui-mode.md`, `docs/audit-ui-expert-frameworks.md`, `docs/audit-ui-decision-rubric.md`, `docs/audit-ui-completeness-gate.md`, and `docs/audit-ui-visual-artifact-contract.md`. For `/audit-ui`, generate 5–7 improvement ideas, select 3 concepts, and show 3 visual concept images in chat using image generation when available. If image generation is unavailable, explicitly write `VISUAL_ARTIFACT_UNAVAILABLE` and provide 3 structured wireframes. Do not implement code until I choose Concept A/B/C or run `/delivery`.
```

Short version:

```txt
/audit-ui: read shared docs from `andylitvinov-design/reiki-yggdrasil`, propose 5–7 ideas, select 3 concepts, show 3 visual concept images if image generation is available; otherwise write `VISUAL_ARTIFACT_UNAVAILABLE` + 3 wireframes. No code until Concept A/B/C is chosen or `/delivery` is run.
```
